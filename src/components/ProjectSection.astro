---
import "../styles/global.css";

const {
  marque,
  presentation,
  description,
  tags = [],
  images = []
} = Astro.props;
---

<main class="md:pt-10">
  <section class="text-black md:pl-6 md:pr-6 relative">
    <div class="bg-white border-2 border-green-max rounded-2xl p-4 md:p-10 mx-auto top-0 relative">
          <svg class="absolute top-0 left-0 w-full h-full z-0" viewBox="0 0 1440 320" preserveAspectRatio="none">
      <path
        fill="#36f4a4"
        fill-opacity="0.1"
        d="M0,96L48,122.7C96,149,192,203,288,192C384,181,480,107,576,85.3C672,64,768,96,864,128C960,160,1056,192,1152,213.3C1248,235,1344,245,1392,250.7L1440,256L1440,0L1392,0C1344,0,1248,0,1152,0C1056,0,960,0,864,0C768,0,672,0,576,0C480,0,384,0,288,0C192,0,96,0,48,0L0,0Z"
      ></path>
    </svg>
      
      <!-- Titre -->
      <div class="md:flex justify-between items-center mb-6 relative">
        <h2 class="text-2xl font-serif">
          {marque} • <span class="font-title text-lg">{presentation}</span>
        </h2>
        <button
  class="relative font-title font-medium cursor-pointer opacity-90 hover:opacity-100 transition-opacity p-[2px] bg-black rounded-[16px] bg-gradient-to-t from-[#041E18] to-[#01F9C0] active:scale-95"
>
  <span
    class="w-full h-full flex items-center gap-2 px-8 py-3 bg-[#B931FC] text-white rounded-[14px] bg-gradient-to-t from-[#01F9C0] to-[#041E18]"
  >
    <svg xmlns="http://www.w3.org/2000/svg" width="25" height="25" fill="currentColor" class="bi bi-arrow-right-short -rotate-45" viewBox="0 0 16 16">
  <path fill-rule="evenodd" d="M4 8a.5.5 0 0 1 .5-.5h5.793L8.146 5.354a.5.5 0 1 1 .708-.708l3 3a.5.5 0 0 1 0 .708l-3 3a.5.5 0 0 1-.708-.708L10.293 8.5H4.5A.5.5 0 0 1 4 8"/>
</svg>Visiter</span
  >
</button>

      </div>

      <!-- Contenu principal -->
      <div class="md:grid md:grid-cols-2 gap-8 relative">

        <!-- Gauche -->
        <div class="flex flex-col">
          
          <!-- Tags -->
          <div class="flex flex-wrap gap-4 mb-6">
            {tags.map(tag => (
              <span class="bg-green-max rounded-2xl py-2 px-4 text-sm">
                {tag}
              </span>
            ))}
          </div>

          <!-- Description -->
          <p class="font-title text-left justify-center">
            {description}
          </p>
        </div>

        <!-- Droite : Images -->
        {images.length > 0 && (
          <div class="grid gap-4">
            {images.map(img => (
              <img
                src={img}
                alt={marque}
                class="mt-10 md:mt-0 object-cover object-top w-full h-96 rounded-xl border-2 border-gray-400/50"
              />
            ))}
          </div>
        )}
      </div>
    </div>
  </section>
</main>