---
import "../styles/global.css";
import AnimatedSection from "../components/AnimatedSection.jsx";
import React from "react";
import { motion } from "framer-motion";
---

<section
  class="relative flex flex-col justify-center items-center text-center min-h-screen overflow-hidden bg-green-dark text-white grain"
>
  <div class="absolute inset-0 z-0">
    <div
      class="absolute top-1/3 left-1/2 -translate-x-1/2 w-[600px] h-[600px] bg-green-max/20 blur-[150px] rounded-full"
    >
    </div>
  </div>

  <div
    class="relative z-10 md:w-3/4 w-11/12 mx-auto flex flex-col items-center space-y-12"
  >
    <div class="space-y-8 px-4">
      <motion.h1
        className="text-4xl md:text-7xl font-title font-extrabold leading-snug"
        initial={{ y: -100, opacity: 0 }}
        animate={{ y: 0, opacity: 1 }}
        transition={{
          type: "spring",
          stiffness: 50, // Moins rigide
          damping: 20, // Amorti rapidement
          delay: 0.3, // Petit délai au chargement
        }}
        client:visible
      >
        <span class="text-white">Expert E-commerce & Web-Marketing Digital</span
        >
      </motion.h1>

      <div class="space-y-4 max-w-4xl mx-auto">
        <p
          class="text-xl md:text-2xl text-white/90 font-light font-body leading-relaxed"
        >
          Je transforme vos idées en solutions digitales performantes
        </p>

        <div
          class="flex flex-col md:flex-row gap-4 justify-center items-center text-base md:text-lg"
        >
          <div
            class="flex items-center gap-2 bg-green-max/20 px-5 py-2.5 rounded-full border border-green-max/30"
          >
            <span class="text-2xl">💼</span>
            <span class="font-medium">Disponible en freelance</span>
          </div>
          <div class="hidden md:block text-green-max/50">•</div>
          <div
            class="flex items-center gap-2 bg-green-max/20 px-5 py-2.5 rounded-full border border-green-max/30"
          >
            <span class="text-2xl">🎯</span>
            <span class="font-medium"
              >En recherche CDI/CDD - Rennes & Lannion</span
            >
          </div>
        </div>
      </div>

      <div class="flex flex-wrap justify-center gap-3 pt-4">
        <div
          class="bg-btn-max items-center gap-2 px-4 py-2 rounded-full bg-green-max-dark/50 border border-border/50 text-sm font-medium text-foreground hover:border-green-max/50 hover:bg-green-max/5 transition-all duration-300"
        >
          <p>📊 Web-Marketing</p>
        </div>

        <div
          class="bg-btn-max items-center gap-2 px-4 py-2 rounded-full bg-green-max-dark/50 border border-border/50 text-sm font-medium text-foreground hover:border-green-max/50 hover:bg-green-max/5 transition-all duration-300"
        >
          <p>✍️ Communication</p>
        </div>

        <div
          class="bg-btn-max items-center gap-2 px-4 py-2 rounded-full bg-green-max-dark/50 border border-border/50 text-sm font-medium text-foreground hover:border-green-max/50 hover:bg-green-max/5 transition-all duration-300"
        >
          <p>💻 Développement Web</p>
        </div>
      </div>
    </div>

    <div class="flex justify-center gap-4 md:gap-6 pt-6">
      <a
        href="/MaxenceGarandelCV.pdf"
        download
        class="px-8 py-3 rounded-full bg-green-max font-bold bg-btn-max text-[#041E18] hover:bg-green-max/80 transition-all duration-300 shadow-xl shadow-green-max/20 inline-flex items-center justify-center"
      >
        Télécharger mon CV
      </a>
      <a
        href="#projects"
        class="bg-white-max text-green-max-dark rounded-full bg-btn-max border border-green-max flex justify-center items-center gap-2 px-6 py-3 text-sm md:text-base font-semibold"
      >
        Voir mes Réalisations
      </a>
    </div>
  </div>
</section>

<div
  class="absolute bottom-10 left-1/2 -translate-x-1/2 animate-bounce text-green-max opacity-70"
>
</div>
