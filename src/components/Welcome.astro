---
import "../styles/global.css";
import { Icon } from "astro-icon/components";
---

<section class="relative flex flex-col justify-center items-center text-center min-h-screen overflow-hidden bg-[#041E18] text-white">

  <!-- Glow / gradient background -->
  <div class="absolute inset-0 z-0">
    <div class="absolute top-1/3 left-1/2 -translate-x-1/2 w-[600px] h-[600px] bg-[#36f4a4]/20 blur-[150px] rounded-full"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 md:w-3/4 w-11/12 mx-auto flex flex-col items-center space-y-12">
    
    <!-- Photo -->
    <div class="relative group">
      <div class="absolute inset-0 rounded-full blur-xl bg-[#36f4a4]/30 opacity-60 group-hover:opacity-80 transition duration-500"></div>
      <img
        src="/pdp.png"
        alt="Portrait de Maxence"
        class="relative h-44 w-44 object-cover rounded-full mx-auto shadow-[0_0_40px_10px_rgba(54,244,164,0.4)] border-4 border-[#36f4a4]/70 transition-transform duration-700 group-hover:scale-105"
      />
    </div>

    <!-- Texte principal -->
    <div class="space-y-6 px-4">
      <h1 class="text-4xl md:text-6xl font-title font-bold leading-tight">
        Hello ! C’est <span class="font-serif text-6xl md:text-7xl text-green-max">Maxence 👋</span>
      </h1>

      <p class="text-lg md:text-xl max-w-2xl mx-auto text-white/80 font-body leading-relaxed">
          <span class="text-green-max">Assistant e-commerce</span> et <span class="text-green-max">développeur web</span> spécialisé en Shopify et site web vitrine
      </p>

      <div class="flex justify-center gap-2">
  
  <!-- Shopify -->
  <div class="bg-green-50 text-black rounded-full border-2 border-green-max-dark flex justify-center items-center gap-2 px-4 py-1">
    <p>Shopify</p>
    <Icon 
      name="shopify"
      class="w-4 md:w-6 h-full text-green-max-dark" />
  </div>

  <!-- Dev -->
  <div class="bg-green-50 text-black rounded-full border-2 border-green-max-dark flex justify-center items-center gap-2 px-4 py-1">
    <p>Dev Web</p>
    <Icon 
      name="dev"
      class="w-4 md:w-6 h-full text-green-max-dark" />
  </div>

</div>

      </div>

      <!-- Boutons d’action -->
      <div class="flex justify-center gap-6 pt-4">
        <a
          href="#projects"
          class="px-6 py-3 rounded-full font-medium bg-green-max text-[#041E18] hover:bg-green-max/80 transition-all duration-300"
        >
          Voir mes projets
        </a>
        <a
          href="#contact"
          class="px-6 py-3 rounded-full font-medium border border-green-max text-green-max hover:bg-green-max hover:text-[#041E18] transition-all duration-300"
        >
          Me contacter
        </a>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-10 left-1/2 -translate-x-1/2 animate-bounce text-green-max opacity-70">
    <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
    </svg>
  </div>

</section>
<hr class="border-white border-4">
