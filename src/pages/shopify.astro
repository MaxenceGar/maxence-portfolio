---
import "../styles/global.css";
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <!-- Hero Section -->
  <section class="relative pt-32 pb-20 overflow-hidden">
    <!-- Background Elements -->
    <div
      class="absolute top-0 left-1/2 -translate-x-1/2 w-full h-full max-w-7xl pointer-events-none z-0"
    >
      <div
        class="absolute top-20 left-1/4 w-96 h-96 bg-green-max/10 rounded-full blur-3xl mix-blend-screen"
      >
      </div>
      <div
        class="absolute bottom-10 right-1/4 w-72 h-72 bg-green-secondaire/20 rounded-full blur-3xl mix-blend-screen"
      >
      </div>
    </div>

    <div
      class="absolute top-96 left-44 rounded-full bg-[#1f2937] border-[#374151] border-1 py-2 px-4 flex gap-2 items-center justify-center"
    >
      <p class="text-xl font-title">🎤</p>
      <p class="text-xl font-title">Dev Shopify</p>
    </div>

    <div
      class="animate-float-slow absolute bottom-96 right-44 rounded-full bg-[#1f2937] border-[#374151] border-1 py-2 px-4 flex gap-2 items-center justify-center"
    >
      <p class="text-xl font-title">🎤</p>
      <p class="text-xl font-title">Custom Theme</p>
    </div>

    <div class="container mx-auto px-6 relative z-10">
      <div class="flex flex-col items-center">
        <!-- Image Container with Glow & Icons -->
        <div class="relative group">
          <!-- Glow Effect -->
          <div
            class="absolute inset-0 bg-green-max rounded-full blur-2xl opacity-20 group-hover:opacity-40 transition-opacity duration-500"
          >
          </div>

          <img
            src="/images/description-moi.webp"
            alt="Maxence Garandel"
            class="relative w-64 h-64 md:w-80 md:h-80 object-cover rounded-full border-4 border-green-max/50 shadow-2xl shadow-green-max/20 transition-transform duration-500 ease-out group-hover:scale-105"
          />

          <!-- Floating Icons (Responsive positioning) -->
          <div
            class="absolute -left-4 top-10 md:-left-12 md:top-12 animate-float-slow"
          >
            <div class="hover:scale-110 transition-transform duration-300">
              <img
                src="/icon/shopify-icon-custom.svg"
                alt="Shopify"
                class="w-8 h-8 md:w-24 md:h-24"
              />
            </div>
          </div>

          <div
            class="absolute -right-4 bottom-10 md:-right-12 md:bottom-12 animate-float-delayed"
          >
            <div class="hover:scale-110 transition-transform duration-300">
              <img
                src="/icon/shopify-icon-custom.svg"
                alt="Shopify"
                class="w-8 h-8 md:w-24 md:h-24"
              />
            </div>
          </div>
        </div>

        <!-- Title -->
        <h1
          class="mt-12 text-center text-5xl md:text-7xl font-serif font-medium text-white-max tracking-tight"
        >
          Expert <span class="text-green-max italic">Shopify</span>
        </h1>
        <p
          class="mt-6 text-center text-gray-400 max-w-2xl text-lg md:text-xl font-light"
        >
          De la création de marque au développement technique, une passion
          devenue expertise.
        </p>
      </div>
    </div>
  </section>

  <!-- Story Section -->
  <section class="py-20 relative">
    <div class="container mx-auto px-6">
      <div class="max-w-4xl mx-auto">
        <div
          class="bg-green-secondaire/10 border border-green-max/10 rounded-3xl p-8 md:p-12 backdrop-blur-sm relative overflow-hidden"
        >
          <!-- Decorative quote mark -->
          <div
            class="absolute top-6 left-8 text-9xl font-serif text-green-max/5 leading-none select-none"
          >
            "
          </div>

          <h2
            class="text-3xl md:text-4xl font-serif font-medium mb-8 text-white-max relative z-10"
          >
            Comment j'ai découvert Shopify ?
          </h2>

          <div
            class="space-y-6 text-gray-300 leading-relaxed text-lg relative z-10"
          >
            <p>
              Tout a commencé en <span class="text-green-max font-semibold"
                >2018</span
              >. Je voulais lancer ma propre marque de vêtements. J'avais les
              visuels, j'avais trouvé l'imprimeur, mais il me manquait la pièce
              maîtresse : <span class="text-white-max"
                >la boutique en ligne</span
              >.
            </p>
            <p>
              C'est là que j'ai découvert Shopify. Ce qui n'était au départ
              qu'un outil est vite devenu une passion. J'ai passé des nuits à
              comprendre le fonctionnement des thèmes, du Liquid, et de
              l'écosystème e-commerce.
            </p>
            <p>
              Aujourd'hui, je ne vends plus de vêtements, mais je construis des
              expériences d'achat performantes pour ceux qui le font.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Timeline Section -->
  <section class="py-200 pb-32">
    <div class="container mx-auto px-6">
      <h2
        class="text-center text-4xl md:text-5xl font-serif font-medium mb-16 text-white-max"
      >
        Mon parcours
      </h2>

      <div class="max-w-3xl mx-auto relative">
        <!-- Vertical Line -->
        <div
          class="absolute left-1/2 transform -translate-x-1/2 h-full w-px bg-gradient-to-b from-green-max/0 via-green-max/50 to-green-max/0"
        >
        </div>

        <!-- Timeline Item 1 -->
        <div
          class="relative flex items-center justify-between w-full mb-12 group"
        >
          <!-- Left Content (Date) -->
          <div class="w-5/12 text-right pr-8">
            <h3
              class="text-3xl font-serif text-green-max group-hover:text-white-max transition-colors duration-300"
            >
              2018
            </h3>
          </div>

          <!-- Center Dot -->
          <div
            class="absolute left-1/2 transform -translate-x-1/2 w-4 h-4 bg-green-max-dark border-2 border-green-max rounded-full z-10 group-hover:scale-125 group-hover:bg-green-max transition-all duration-300 shadow-[0_0_10px_rgba(54,244,164,0.5)]"
          >
          </div>

          <!-- Right Content (Description) -->
          <div class="w-5/12 pl-8">
            <h4 class="text-xl font-bold text-white-max mb-1">La Découverte</h4>
            <p class="text-gray-400 text-sm">
              Création de ma première boutique et immersion dans l'écosystème
              Shopify.
            </p>
          </div>
        </div>

        <!-- Placeholder for future items (Visual balance) -->
        <div
          class="relative flex items-center justify-between w-full group opacity-50 hover:opacity-100 transition-opacity duration-300 mb-12"
        >
          <div class="w-5/12 text-right pr-8">
            <h3 class="text-3xl font-serif text-gray-600">2022</h3>
          </div>
          <div
            class="absolute left-1/2 transform -translate-x-1/2 w-3 h-3 bg-gray-700 border-2 border-gray-500 rounded-full z-10"
          >
          </div>
          <div class="w-5/12 pl-8">
            <h4 class="text-xl font-bold text-gray-300 mb-1">
              Début de mon stage chez Ohmycase
            </h4>
            <p class="text-gray-500 text-sm">
              Durant mon BTS Communication, j'ai effecuté mon stage chez
              Ohmycase.
            </p>
          </div>
        </div>
        <div
          class="relative flex items-center justify-between w-full group opacity-50 hover:opacity-100 transition-opacity duration-300"
        >
          <div class="w-5/12 text-right pr-8">
            <h3 class="text-3xl font-serif text-gray-600">Aujourd'hui</h3>
          </div>
          <div
            class="absolute left-1/2 transform -translate-x-1/2 w-3 h-3 bg-gray-700 border-2 border-gray-500 rounded-full z-10"
          >
          </div>
          <div class="w-5/12 pl-8">
            <h4 class="text-xl font-bold text-gray-300 mb-1">
              Expert Freelance
            </h4>
            <p class="text-gray-500 text-sm">
              Accompagnement de marques sur mesure.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <style>
    /* Custom Animations defined locally to avoid touching global css */
    @keyframes float {
      0%,
      100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }
    .animate-float-slow {
      animation: float 6s ease-in-out infinite;
    }
    .animate-float-delayed {
      animation: float 6s ease-in-out 3s infinite;
    }
  </style>
</Layout>
